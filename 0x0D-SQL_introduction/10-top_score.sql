-- List all records of the table second_table.
-- Resulsts should display both the score and the name.
-- Records should be ordered by score (top first).
SELECT `score`, `name`
FROM  `second_table`
ORDER BY `score` DESC;
